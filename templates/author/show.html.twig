<!DOCTYPE html>
<html>
<head>
    <title>Author Page</title>
</head>
<body>
    <h1>Author Name: {{ name }}</h1>
</body>
</html>
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Détails de l'Auteur</title>
</head>
<body>
    <h1>{{ author.username|upper }}</h1>
    <img src="{{ author.picture }}" alt="{{ author.username }}" width="150" height="150"><br>
    <p>Email: {{ author.email }}</p>
    <p>Nombre de livres: {{ author.nb_books }}</p>
    <a href="{{ path('list_authors') }}">Retour à la liste</a>
</body>
</html>
